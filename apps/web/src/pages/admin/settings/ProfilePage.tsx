import { Settings } from 'lucide-react';
import EmptyShell from '@/components/EmptyShell';
export default function ProfilePage() {
  return <EmptyShell title="Profile & Settings" subtitle="Update your business profile and account settings." icon={Settings} />;
}
